server:
  port: ${PORT:8080}
  forward-headers-strategy: framework
  servlet:
      context-path: /

spring:
  datasource:
    url: mysql://localhost:3306/account
    password: 1234
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
