server:
  port: ${PORT:8080}
  forward-headers-strategy: framework
  servlet:
      context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:12345/account
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      username: root
      password: 1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: 'validate'
